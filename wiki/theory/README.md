# wiki/theory/

Theory concept cards, source summaries, and algorithm dependency maps.

- `concept_cards/` — individual concept articles with YAML frontmatter
- `source_summaries/` — summaries of ingested theory sources
- `algorithm_dependencies/` — mappings from theory concepts to product/algorithm logic
